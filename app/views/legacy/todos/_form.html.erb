<%= form_with(model: legacy_todo) do |form| %>
  <% if legacy_todo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(legacy_todo.errors.count, "error") %> prohibited this legacy_todo from being saved:</h2>

      <ul>
        <% legacy_todo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :detail %>
    <%= form.text_field :detail %>
  </div>

  <div class="field">
    <%= form.label :is_done %>
    <%= form.check_box :is_done %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
